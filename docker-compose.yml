services:
  app:
    image: php:8.2-fpm
    volumes:
      - ./src:/var/www
    working_dir: /var/www

  nginx:
    image: nginx:1.25
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
